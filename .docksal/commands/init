#!/usr/bin/env bash

## Initialise the project
##
## Usage: fin init

fin start

git clone --branch 7.x https://git.drupal.org/project/drupal.git --depth 1 core

cp -R sites core

cd core

fin drush site-install -y --account-pass=admin
fin uli
